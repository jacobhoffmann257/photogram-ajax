var request = $(".<%= dom_id(@follow_request.recipient)%>_follow_request");
var updated = $("<%= j(render 'follow_requests/follow_unfollow', sender: @follow_request.sender, recipient: @follow_request.recipient )%>");
request.replaceWith(updated);
